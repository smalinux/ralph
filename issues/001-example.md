# 001 — Example issue (replace with real prd-to-issues output)

<!--
  PLACEHOLDER. Real issue files are generated by your prd-to-issues skill; match
  whatever shape it emits. The one field the loop relies on is the AFK/HITL tag
  below — prompt.md says Ralph works AFK issues only, never HITL ones.
-->

**Mode:** AFK        <!-- AFK = Ralph works it unattended · HITL = human-in-the-loop only -->
**Priority:** Development infrastructure   <!-- see prompt.md TASK SELECTION order -->

## Goal
<One or two sentences: the outcome this issue delivers.>

## Details
<Context and constraints; link back to the PRD section this came from.>

## Acceptance criteria
- [ ] <observable, testable outcome>
- [ ] Tests cover the new behaviour and pass (`npm run test`)
- [ ] Type checks pass (`npm run typecheck`)

<!-- On completion Ralph moves this file to issues/done/.
     If only partly done, Ralph appends a note here for the next iteration. -->
